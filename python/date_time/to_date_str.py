
def to_date_str(date_obj, format='%Y-%m-%d'):
    return date_obj.strftime(format)


def to_time_str(time_obj, format='%H:%M:%S'):
    return time_obj.strftime(format)


def to_datetime_str(datetime_obj):
    pass
